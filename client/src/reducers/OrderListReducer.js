const initialState = {
    res_id:[{}]
}
const orderListReducer = (state=initialState,action)=>{
  switch(action.type){
    case "PLACE_ORDER":

    return state
    default:
    return state
  }
}

export default orderListReducer
